<div style="max-width: 650px; margin: 0 auto;">
  <h2 style="margin-bottom: 0.5rem;">Edit <%= @person.name %></h2>
  <p style="color: var(--pico-muted-color); margin-bottom: 1.5rem;">Update allowance and calendar settings.</p>

  <%= render "form", person: @person %>
</div>
