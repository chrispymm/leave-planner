<div style="max-width: 650px; margin: 0 auto;">
  <h2 style="margin-bottom: 0.5rem;">Edit <%= @additional_calendar.name %></h2>
  <p style="color: var(--pico-muted-color); margin-bottom: 1.5rem;">Update the name, description or highlight colour.</p>

  <%= render "form", additional_calendar: @additional_calendar %>
</div>
